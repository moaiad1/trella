# Dev: leave unset so requests go to /api and Vite proxies to FastAPI (vite.config.ts).
# If you set a full URL, use either:
#   VITE_API_BASE_URL=http://127.0.0.1:8000/api
# or (app will append /api automatically):
#   VITE_API_BASE_URL=http://127.0.0.1:8000
